{
  "name": "autorename",
  "version": "1.0.0",
  "description": "",
  "main": "readDocxTitle.js",
  "bin": "dist/main.node.js",
  "pkg": {
    "targets": [
      "node12-win-x64"
    ],
    "outputPath": "dist"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && npx esbuild main.qjs.ts --bundle --external:os --external:std --platform=browser --outfile=dist/main.js --format=esm && node moveImportsToTop.ts",
    "build:min": "npx tsc && npx esbuild dist/main.js --minify --bundle --external:os --external:std --platform=browser --outfile=dist/main.min.js --format=esm",
    "build:nodejs": "npx tsc && npx esbuild main.ts --bundle --platform=node --outfile=dist/main.node.js --format=cjs --target=node18"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
  },
  "devDependencies": {
    "jszip": "^3.10.1",
    "minimist": "^1.2.8",
    "slimdom": "^4.3.5",
    "uzip": "^0.20201231.0",
    "@types/node": "^24.3.0",
    "@types/uzip": "^0.20201231.2",
    "typescript": "^5.9.2"
  }
}